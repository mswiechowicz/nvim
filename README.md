# NeoVim config

#### Copy all files to
``` ~/.config/nvim ```
#### Install all packages with Packer:
``` 
cd ~/.config/nvim 
nvim packer.lua

-- go shift+: in nvim and type

:so
:PackerSync
```

#### Formatting
``` <leader>ff ```
